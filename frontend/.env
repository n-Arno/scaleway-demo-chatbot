# MODEL are used here for frontend presentation, the real model used is defined in the chatbot app

# The OpenAI model to use.
MODEL=gpt-3.5-turbo-0125

# The OpenAI model to use (hardcode to front-end artifact).
NEXT_PUBLIC_MODEL=gpt-3.5-turbo-0125

# The backend API for chat endpoint (here, the frontend will be embedded in the backend)
NEXT_PUBLIC_CHAT_API=/api/chat

